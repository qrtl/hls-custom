Adds a total quantity row to the purchase order report, showing sums for both primary
and secondary units grouped by unit type.

Uses `t-field` rendering so that `report_qweb_field_option` can control decimal
precision per UoM.
