This module adjusts several stock-related views. Key changes:

- The lot_name field in the stock move line operations tree is invisible when
  using the OCA module stock_picking_auto_create_lot, since users will no
  longer enter lot numbers manually during receipt operations.
