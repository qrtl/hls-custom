Go to Inventory > Reporting > Valuation, and use Removal Date to filter the
valuation layers, either from the search panel or with a custom filter. The
field can also be displayed as an optional column of the valuation list.

The removal date is written on a valuation layer when the layer is created, and
is realigned afterwards when the removal date of the lot itself is changed.
