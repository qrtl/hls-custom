Set the timezone on the Company’s partner to control how the Removal DT Date on
lots/serials is computed from the Removal Date (datetime). If no timezone is set,
the Japan timezone is used by default.

The OCA partner_tz module can be used to display the timezone field on the
partner form.
